export const environment = {
    production: true,
    urlBase: `http://${location.hostname}:${8000}`,
    urlSocket: `ws://${location.hostname}:${7000}`,

    secretKeyCrypto: "u17A8O=nECXf+NQmMkDa0~£"
};
